# Dev Tasks — 2026-03-27
_Issued by CEO. Both items are batched — complete together, single QA pass._

---

## Task 1: Align composite confidence in WS crypto path

**File:** `position_monitor.py` — `evaluate_crypto_entry()`

**Problem:**
The WS intraday entry path uses a hardcoded 2-tier confidence stub:
```python
confidence = 0.60 if abs(edge) >= 0.20 else 0.50
```

The scheduled scan path in `crypto_client.py` was already updated (Opus audit, 2026-03-26) to use a real composite:
- Edge magnitude: 50%
- Bid-ask spread: 30%
- Time to settlement: 20%

**Fix:** Apply the same composite confidence logic to `evaluate_crypto_entry()` in `position_monitor.py`. The function already has all the inputs needed (`edge`, `yes_ask`, `yes_bid`, `hours_left`). Pull the shared helper from `crypto_client.py` — do not duplicate the logic.

**Acceptance:** `confidence` field in WS-triggered trade logs reflects the composite value, not a flat 0.50/0.60.

---

## Task 2: Add WebSocket as a persistent background process (15-min crypto on hold)

**File:** `position_monitor.py`, Task Scheduler

**Problem:**
The WS live-entry path (`position_monitor.py`) is not running as a persistent background process. Today's crypto trades came exclusively from scheduled `ruppert_cycle.py` scans. There are no intraday real-time crypto entries between scan windows.

The current `Ruppert-PostTrade-Monitor` task runs `position_monitor.py` on a 15-min repeat, but this appears to be firing it as a short-lived polling run — not a persistent WS session.

**Investigation needed first:**
- Confirm how `position_monitor.py` is actually being invoked (polling mode vs WS mode)
- Check if `--ws` or equivalent flag exists, or if WS is always attempted
- Check if WS connection is failing silently and falling back to polling

**Fix (after investigation):**
- If WS is failing: diagnose and fix the connection/auth issue
- If WS is never being attempted: wire up a persistent WS task (separate from the 15-min polling task) that runs continuously during market hours (6AM–11PM)
- Do NOT mix the persistent WS session with the post-trade polling loop — they should be separate concerns

**Out of scope:** 15-min crypto markets (KXBTC15M etc.) — separate task, on hold.

**Acceptance:** Activity log shows `[WS]` connection and real-time tick events firing between scheduled scan windows.

---

## Notes for QA
- Test Task 1 in DEMO — verify confidence values in `trades_YYYY-MM-DD.jsonl` are composite, not flat
- Test Task 2 in DEMO — confirm WS connects and logs ticks without crashing; confirm fallback to polling if WS fails
- Both tasks must pass QA before CEO review
