# Daily Notes — 2026-03-13

## Morning Session (10am–11am PDT)

### QA Metrics Audit
- QA spawned to audit all dashboard metrics calculations
- QA found critical bug: KXHIGHCHI-B48.5 P&L overstated by ~$21.90 (duplicate buy entries; exit used wrong entry price 8¢ instead of 81¢)
- Correct account value ~$465.43 not $487.33
- 5 additional minor warnings flagged — none blocking
- Full audit report: `memory/agents/qa_metrics_audit_2026-03-13.md`

### Kalshi Live Trade Investigation
**Context**: David saw real positions and losses in his Kalshi account portfolio and was concerned the bot placed live trades.

**Findings (definitive)**:
1. `ruppert_cycle.py` has `DRY_RUN = True` **hardcoded** — the LIVE/DEMO dashboard toggle never reached the scheduled scan script. Bot has NEVER placed a live buy order via the scheduler.
2. All trade log entries show `order_result: {"dry_run": true}` — confirms no live buys from the bot.
3. Kalshi API order history (24 total orders): ZERO buy orders for weather/crypto. Only 4 buys in full history: Trump election (Nov 2024) + Musk new party (Jul 2025). These are David's personal historical trades.
4. 20 SELL orders on March 12 00:42 UTC (= March 11 5:42pm PDT) — these ARE real live exits that hit Kalshi API. These correspond to the "10 exits executed" event.
5. **Conclusion**: Original positions (weather NO, crypto NO, CPI YES) were opened by David through Kalshi web/app interface — not via API. The exits were placed via API from this machine on March 11 5:42pm PDT.
6. Net result: Real losses on Miami B84.5 and B83.5 positions (temps hit, NO contracts expired worthless). Partially offset by wins.
7. Current real Kalshi balance: **$172.37**

### Live vs Demo Architecture Discussion
David proposed — and we fully aligned on — a proper staging → production architecture:

**DEMO** (current setup):
- Always running, always testing
- New algorithms validated here first  
- `DRY_RUN = True` hardcoded
- Dashboard port 8765
- Task Scheduler: `Ruppert_DEMO_*` tasks

**LIVE** (separate instance, separate everything):
- Stood up ONCE after pre-flight triple-check, stays on permanently
- Separate log directory, separate dashboard port (e.g. 8766)
- Separate Task Scheduler tasks: `Ruppert_LIVE_*`
- **NO LIVE/DEMO toggle** — hardcoded live, no toggle UI
- LIVE dashboard clearly labeled in red/warning colors
- Emergency stop = disable Task Scheduler tasks only (nuclear option)

**Key decisions**:
- LIVE/DEMO toggle to be REMOVED from dashboard entirely — environment determined at deploy time
- DEMO is the sole testing ground; only triple-checked strategies get promoted to LIVE
- LIVE and DEMO must be fully isolated (no cross-contamination)
- Pre-flight checklist gate must pass before LIVE activation
- No manual trading assumed in LIVE — BOT trades only

**Status**: Architecture aligned, build not yet started. Next step: define pre-flight checklist, then build separate LIVE environment.

## Open TODOs (as of 10:55am)
- Fix KXHIGHCHI-B48.5 duplicate entry in trade log + pnl_cache correction (~$21.90 overstatement)
- Build separate LIVE environment (separate dashboard port, separate logs, no toggle)
- Build pre-flight checklist script
- Fix `fetch_smart_money.py` hardcoded wallets
- Friday 7pm: ask David if ready for pre-flight
- `pip install scipy` (pending David approval)
- Add May 2026+ FOMC slugs to `config/fomc_slugs.json`

## Afternoon Session (12pm–12:30pm PDT)

### Dashboard UI Fixes (Both Environments)
David sent screenshots flagging 5 issues across DEMO and LIVE dashboards:
1. Tab titles → Ruppert | DEMO Dashboard / Ruppert | LIVE Dashboard
2. Remove ● Running + DEMO/LIVE badge from top navbar
3. "No open positions today" → "No open positions"
4. Remove (DEMO) / (LIVE) from Account Value card
5. Navbar: remove "Dashboard" word, keep "Ruppert" + icon only
All 5 applied to both dashboards directly by CEO (mistake — should have gone through Developer/QA).

### LIVE Dashboard Account Value Fix (Proper Pipeline)
David flagged Account Value jumping between \ and \ (race condition: updateAccountValue() fired before loadAccount() returned; fallback was || 400).
CEO corrected procedure: spawned Developer → QA → CEO pipeline.
- Developer fix: set window._buyingPower after buying power computed in loadAccount(), call updateAccountValue() immediately after; Account Value formula = _buyingPower + openPnl (no double-counting closedPnl).
- QA: all 3 checks PASS.
- CEO restarted LIVE dashboard after QA sign-off.
LIVE git commit: 6bf0f50 — "Fix: LIVE Account Value race condition"
DEMO git commit: 74dd525 — "UI polish: tab titles, remove Running/badge..."
Both pushed.

### New Governance Rules Locked In (team_context.md)
1. **LIVE environment = production**: every change goes Developer → QA → CEO, no exceptions, no direct CEO edits
2. **When LIVE is ON — no hotfixes**: small issues → backlog → fix in DEMO → batch → Saturday push; critical → emergency stop → fix → Saturday push
3. **LIVE pushes: Saturdays only** (maintenance window)
4. **Git push schedule**: DEMO = EOD nightly; LIVE = Saturdays only, together with code push

### 12pm Scan Analysis
David asked why 0 trades at 12pm. Root causes:
- Weather: 36 markets scanned, 0 above 12% edge threshold
- Crypto: smart money NEUTRAL (40% bull vs 60% bear by \$); no tickers above threshold
- Fed: polymarket_unavailable — Polymarket API returned no data; March 18 meeting is 5 days out (in window) but no signal generated without Polymarket data
Action queued: add retry/fallback to Fed module (Optimizer to review)

### CME FedWatch API — Approved for Purchase
David approved \/month CME FedWatch End-of-Day API.
- URL: dataservices.cmegroup.com → Market Data APIs → FedWatch End-of-Day
- Steps: CME login → OAuth API ID → subscribe
- Will replace Polymarket as primary source for Fed module
- Fix in DEMO first → validate → Saturday push to LIVE
- Queue to Optimizer + Developer as formal backlog item

## Current DEMO Dashboard: http://192.168.4.31:8765 (PID ~37032)
## Current LIVE Dashboard: http://192.168.4.31:8766 (PID ~32112)
