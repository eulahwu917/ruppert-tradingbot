# QA Report — Batch 1 — 2026-03-27b

**Agent:** SA-4 QA  
**Date:** 2026-03-27  
**Reviewing:** dev-batch1-2026-03-27b  
**Overall Result:** ✅ PASS

---

## Check 1: 1c Ghost Market Filter

### config.py
- `MIN_YES_ASK = 5` ✅ — Present, placed after `MIN_CONFIDENCE` block
- `MAX_MODEL_MARKET_DIVERGENCE = 0.70` ✅ — Present, placed immediately after `MIN_YES_ASK`
- Placement is logical and grouped with confidence/risk thresholds ✅

### edge_detector.py — Gate 1 (Penny gate)

Actual code in `analyze_market()`:
```python
if not yes_ask or yes_ask <= 0:
    return None

# ── Ghost/penny market filter ─────────────────────────────────────────────
if yes_ask < config.MIN_YES_ASK:
    logger.debug(
        f"[Edge] {ticker}: skipping — yes_ask={yes_ask}c below "
        f"MIN_YES_ASK={config.MIN_YES_ASK}c"
    )
    return None
```

- Placed AFTER `yes_ask <= 0` guard ✅
- Returns `None` + logs at `DEBUG` level ✅
- Comment header clearly labels the section ✅

### edge_detector.py — Gate 2 (Divergence gate)

Actual code:
```python
# ── Model/market divergence gate ─────────────────────────────────────────
_divergence_gap = abs(model_prob - market_prob)
if _divergence_gap > config.MAX_MODEL_MARKET_DIVERGENCE:
    logger.debug(
        f"[Edge] {ticker}: skipping — model/market divergence "
        f"{_divergence_gap:.0%} exceeds threshold"
    )
    return None

# ── Edge calculation ──────────────────────────────────────────────────────
edge = model_prob - market_prob
```

- Placed directly before `edge = model_prob - market_prob` ✅
- Returns `None` + logs at `DEBUG` level ✅
- Note: Gate 2 is placed AFTER the two confidence gates (MIN_ENSEMBLE_CONFIDENCE,
  MIN_CONFIDENCE). Dev spec only requires "before edge calculation" — this satisfies
  the spec. Ordering is acceptable; divergence check after confidence gates is fine
  since we want to skip low-confidence markets first anyway.

### Correctness Trace

| Input | Gate | Expected | Actual |
|---|---|---|---|
| yes_ask=1 | Penny gate | fires → None | ✅ 1 < 5 |
| yes_ask=5 | Penny gate | passes | ✅ 5 < 5 is False |
| yes_ask=4 | Penny gate | fires → None | ✅ 4 < 5 |
| model=0.99, mkt=0.01 → gap=0.98 | Divergence gate | fires → None | ✅ 0.98 > 0.70 |
| model=0.75, mkt=0.10 → gap=0.65 | Divergence gate | passes | ✅ 0.65 < 0.70 |
| model=0.80, mkt=0.10 → gap=0.70 | Divergence gate | passes (boundary) | ✅ 0.70 > 0.70 is False |

Check 1: ✅ **PASS**

---

## Check 2: FOMC Slugs

### config/fomc_slugs.json

Full file contents verified:
```json
{
  "2026-01-29": "fed-decision-january-2026",
  "2026-03-18": "fed-decision-in-march-885",
  "2026-04-29": "fed-decision-april-2026",
  "2026-06-17": "fed-decision-june-2026",
  "2026-07-29": "fed-decision-july-2026",
  "2026-09-16": "fed-decision-september-2026",
  "2026-10-28": "fed-decision-october-2026",
  "2026-12-09": "fed-decision-december-2026"
}
```

- All 8 FOMC dates present ✅
- All 8 slugs are non-null strings ✅
- Dates match canonical 2026 FOMC schedule ✅
- JSON is syntactically valid ✅
- 2026-04-29 corrected from stale "fed-decision-in-april" to "fed-decision-april-2026" ✅
- Previously-null slugs (06-17, 07-29, 09-16, 10-28, 12-09) all filled ✅

### fed_client.py

**Slug loading:**
```python
_FOMC_SLUGS_FILE = Path(__file__).parent / "config" / "fomc_slugs.json"

with open(_FOMC_SLUGS_FILE, encoding="utf-8") as f:
    fomc_slugs: dict = json.load(f)

slug = fomc_slugs.get(date_key)

if not slug:
    # returns no_signal sentinel
```
- File path resolves correctly relative to `fed_client.py` location ✅
- `if not slug:` guard handles future missing/null cases gracefully ✅
- With all slugs now populated, this branch will not fire for any 2026 meeting ✅

**No broken references to null slugs:**
- No hardcoded null checks or `if slug is None` elsewhere in the file ✅
- The `if not slug:` branch was already defensive; no code was relying on null behavior ✅
- Existing `_save_scan_result()` and no_signal sentinel path remains intact ✅

**Kalshi ticker lookup separation:**
- `get_kalshi_fed_markets()` uses `_KalshiClient().get_markets("KXFEDDECISION", ...)` 
  with date string filtering (e.g. "26MAR", "MARCH", "2026") ✅
- No slug lookup anywhere in the Kalshi path ✅
- `_classify_kalshi_outcome()` parses title/ticker text directly — slug-independent ✅

Check 2: ✅ **PASS**

---

## Files Verified

| File | Status |
|---|---|
| `config.py` | ✅ PASS |
| `edge_detector.py` | ✅ PASS |
| `config/fomc_slugs.json` | ✅ PASS |
| `fed_client.py` | ✅ PASS (read-only audit) |

## No Issues Found

No warnings, no anomalies, no unexpected modifications to other code.

---

## Final Verdict: ✅ PASS

Both fixes are correctly implemented and match the dev spec. Ready to commit.
