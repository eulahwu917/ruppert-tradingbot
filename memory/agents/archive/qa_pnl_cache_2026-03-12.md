# QA Report — pnl_cache fix (2026-03-12)
**Reviewer:** SA-4 QA
**Status: PASS**

All five verification points check out cleanly. In `dashboard/api.py` (`get_pnl_history`): `pnl_result = {` is a proper dict assignment at function-body indentation (4 spaces), not an inline return; the dict spans lines 1069–1096 and contains all expected keys including `"closed_pnl"`; the cache write block (`try: ... json.dump({"closed_pnl": round(closed_pnl_total, 2)}, _f) ... except: pass`) executes at function-body level *after* `pnl_result` is fully constructed and is wrapped in a bare-except so it cannot raise; `return pnl_result` is the last statement at the correct 4-space indentation, unconditionally returning the fully-built dict. In `logger.py` (`get_computed_capital`): it correctly checks `os.path.exists(pnl_cache_path)` first, reads `closed_pnl` from the cache with a `float()` cast and a `.get('closed_pnl', 0.0)` default, catches read errors with a warning print and leaves `total_realized_pnl = None`, then falls through to the exit-record fallback only when the cache was absent or unreadable — exactly the intended behavior. No syntax errors, no indentation anomalies, no missing returns, no changes observed outside the two assigned functions. **PASS — safe to proceed to CEO approval.**
