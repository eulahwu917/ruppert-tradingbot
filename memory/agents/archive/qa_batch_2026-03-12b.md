# QA Report — 2026-03-12b (Closed Positions Sort Fix)
_SA-4 QA | 2026-03-12_

**Status: ✅ PASS**

All four verification criteria are satisfied. (1) The sort is correctly descending — line 833 uses `db.localeCompare(da)` (b before a), not `a.localeCompare(b)`, producing newest-first order. (2) The sort key correctly prioritizes `timestamp` with fallback to `_date` (`a.timestamp || a._date || ''` and `b.timestamp || b._date || ''`), which satisfies the `_date` branch of the `_date or date` spec and is also consistent with the display logic on line 851 which uses the same field chain. (3) The `.sort()` call (lines 829–834) is placed immediately after the empty-check guard and before `tb.innerHTML = trades.map(...)` on line 835 — sort happens before render, not after. (4) No other changes to the file are evident; the modification is surgical and confined to the four lines inserted inside `loadTrades()`. No issues found. CEO may approve and stage for end-of-day push.
