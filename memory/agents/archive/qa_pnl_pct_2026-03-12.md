# QA Report — P&L % Display Fix — 2026-03-12

**Agent:** SA-4 QA  
**Commit reviewed:** `ef029e5`  
**Status:** ✅ PASS

---

## Verdict

All six verification criteria pass. In `api.py`, `bot_cost_basis` and `manual_cost_basis` are initialized to `0.0` and accumulated **exclusively** within the `settled_tickers` loop (lines ~907–970), which covers only closed/settled and manually exited positions — the open positions loop (`open_tickers`) never touches these accumulators, so there is no open-position dilution of the denominator. The `is_manual` classification (`src in ('economics', 'geo', 'manual')`) is consistent with the rest of the codebase, correctly routing each position to the right bucket. `position_cost = float(t.get('size_dollars') or 0)` safely handles missing fields. In `index.html`, `setSplitPnl()` now receives `pnl.bot_cost_basis` / `pnl.man_cost_basis` (no more `||1` fallback on `bot_deployed`); the near-zero guard (`Math.abs(cost) < 0.01 → N/A`) is correct and prevents divide-by-zero; the ±999% cap (`Math.max(-999, Math.min(999, pp))` with `*` suffix when triggered) is implemented correctly. One minor note (not a defect): positions that fail the Kalshi API call inside the settled loop hit `continue`, so their `size_dollars` is not added to cost basis — this is actually sound, since if P&L can't be computed for a trade, excluding its cost from the denominator keeps the percentage meaningful. No unrelated code was modified. **Approved for CEO review.**
