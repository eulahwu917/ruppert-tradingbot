# QA Report — P&L % Fix Session 2 (pnl-pct2)
_Date: 2026-03-12_
_Reviewer: SA-4 QA_

**Status: PASS**

Both fixes are correctly implemented in `dashboard/api.py`. **Fix 1 (cost basis accumulation):** `src`, `is_manual`, `position_cost`, and the `bot_cost_basis`/`manual_cost_basis` accumulation block all appear at the very top of the `settled_tickers` loop body — before the `if ticker in exit_records:` branch and before any `continue` statement — confirming that every closed position contributes to cost basis regardless of API success or failure. **Fix 2 (regex):** `settlement_date_from_ticker()` now uses `r'^(\d{2})([A-Z]{3})(\d{2})(\d{2})?$'` with `m.group(1/2/3)` for year/month/day extraction; group 4 (optional hour) is silently discarded, meaning `26MAR1117` → `date(2026, 3, 11)` as expected, and standard 7-char dates like `26MAR11` continue to match unchanged. Notably, `is_settled_ticker()` also carries the same updated regex, keeping both functions consistent. No threshold changes, no identity files, and no files outside `dashboard/api.py` were modified. The existing `except: pass` bare-except patterns are pre-existing and out of scope for this fix. CEO may approve and restart the dashboard server.
