# QA Report — Batch D (2026-03-12)
_SA-4 QA | Reviewed: 2026-03-12 23:18 PDT_

---

## Task 1 — Fed Module: Polymarket as Probability Source (`fed_client.py`)

**Status: ✅ PASS WITH WARNINGS**

### ✅ Checks Passed

1. **CME FedWatch dead code fully removed** — `_parse_fedwatch_json()`, `get_fedwatch_probabilities()`, `_scrape_fedwatch_html()`, and `import re` are absent from the file. No dead code found.

2. **Correct Polymarket endpoint** — `POLYMARKET_FOMC_URL` constant is exactly:
   `https://gamma-api.polymarket.com/markets?tag=fomc&limit=20&active=true`
   Used in `requests.get(POLYMARKET_FOMC_URL, ...)`. ✅

3. **endDate ±5 day filter** — `if abs((end_date - meeting_date).days) <= 5:` — correct bilateral window. ✅

4. **`outcomePrices[0]` as YES probability, scale** — code reads `yes_price = float(outcome_prices[0])`. Comment confirms assumed 0-1 scale. Polymarket gamma API is known to return USDC prices as decimal strings (e.g. `"0.97"`, `"0.03"`), which is 0–1. Assumption appears correct. **See W1.**

5. **`prob_source: 'polymarket'` in signal output** — present in `best_signal` dict and in all skip paths that include `prob_source` (kalshi_markets_unavailable path). ✅

6. **`polymarket_unavailable` skip path writes `fed_scan_latest.json`** — confirmed: the skip block calls `_save_scan_result(_result)` which writes to `logs/fed_scan_latest.json`. W1 from prior QA satisfied. ✅

7. **All thresholds unchanged** — verified in constants:
   - `FED_MIN_EDGE = 0.12` (12%) ✅
   - `FED_MIN_CONFIDENCE = 0.55` (55%) ✅
   - `FED_WINDOW_MIN_DAYS = 2`, `FED_WINDOW_MAX_DAYS = 7` (2–7 day window) ✅
   - `FED_MIN_KALSHI_PRICE = 0.15` (15¢ longshot filter) ✅

8. **No hardcoded API keys** — all data sources (Polymarket gamma, FRED, Kalshi public API) are unauthenticated public endpoints. No credentials in file. ✅

### ⚠️ Warnings

- **W1 — `outcomePrices` scale unverified against live API** (`fed_client.py`, `get_polymarket_fomc_probabilities()`): Developer assumed 0-1 scale. This is consistent with known Polymarket gamma API behavior, but no live validation was done. If the API ever returns 0–100 (e.g., a market returning `"97"` instead of `"0.97"`), probabilities would be wildly wrong (treated as 97× instead of 97%). Recommend validating with a live API call during the first demo run in the signal window.

- **W2 — Local variable `fedwatch_p` retains legacy name** (`fed_client.py`, `get_fed_signal()` loop): The internal loop variable is named `fedwatch_p = polymarket_probs.get(outcome)`. It is NOT exposed in the output dict (stored as `"prob"` in signal, not as `fedwatch_p`). Cosmetic only, no functional impact.

### 🔍 Out-of-Scope Observation

- **`ruppert_cycle.py` Step 4b** — trade log opp dict still contains `"fedwatch_prob": fed_signal.get("prob")` (legacy key name). This is pre-existing code not in scope for this task. Functional (correctly reads `"prob"` from signal), but inconsistent with the rename. CEO may want to queue a cleanup task for Developer.

---

## Task 2 — Scan Notifications (`ruppert_cycle.py`)

**Status: ✅ PASS WITH WARNINGS**

### ✅ Checks Passed

1. **Notification fires at END of full cycle only** — `check` mode exits at `sys.exit(0)` after Step 1, `report` mode exits after its block. The notification block is placed after the `CYCLE COMPLETE` print banner (the very last lines of the file), unreachable from `check`/`report` modes. ✅

2. **Uses `push_alert('warning', ...)` — writes to `pending_alerts.json`** — call is `push_alert('warning', _scan_msg)`. The `push_alert()` function in `ruppert_cycle.py` writes all levels to `ALERTS_FILE = LOGS / 'pending_alerts.json'`. ✅

3. **PDT time calculation — no pytz** — uses `datetime.timezone(timedelta(hours=-7))` via stdlib aliases. No `pytz` import anywhere. ✅ **See W1 for offset accuracy note.**

4. **Fed status reads `logs/fed_scan_latest.json` gracefully** — default value `_fed_status = 'no signal (outside window)'` is set before the try block; file-existence check (`if _fed_latest_path.exists()`) guards the read; outer `except Exception` catches parse errors and sets `_fed_status = 'error reading fed data'`. Missing file, corrupt JSON, and schema mismatches all handled safely. ✅

5. **Buying power formula** — `_bp = max(0.0, round(_capital * 0.70 - _deployed, 2))` — matches spec exactly: `max(0, capital × 0.70 − deployed_today)`. ✅

6. **Entire notification block wrapped in try/except** — top-level `try:` opens before all notification code, `except Exception as _scan_ex: print(f'Scan notify error (non-fatal): {_scan_ex}')` closes it. A crash anywhere in the block (including `push_alert` itself) is caught and printed; cycle does not raise. ✅

7. **No changes to trading logic, thresholds, or cycle steps** — the notification block is appended after `log_cycle('done', summary)` and the final print banner. It is purely read-and-report. All scan/trade variables (`new_weather`, `new_crypto`, `new_fed`, `direction`, `summary`) are already settled before this block. ✅

### ⚠️ Warnings

- **W1 — PDT offset hardcoded to -7h** (`ruppert_cycle.py`, notification block): Will display the wrong time during PST season (clocks fall back, should be -8h, Nov through early Mar). **Currently correct** — as of 2026-03-12, DST resumed 2026-03-08, so -7h is accurate. Developer acknowledged this as low priority. No action needed before go-live on 2026-03-14, but track for winter.

- **W2 — Redundant `timezone` re-import** (`ruppert_cycle.py`, notification block): `from datetime import timezone as _tz, timedelta as _td` is inside the try block. `timezone` is already imported at module level (`from datetime import date, datetime, timezone`). `timedelta` is legitimately new here. The re-import of `timezone` as `_tz` is harmless but unnecessary clutter.

---

## Overall Verdict

| Task | File | Status |
|------|------|--------|
| Task 1 — Polymarket probability source | `fed_client.py` | ✅ PASS WITH WARNINGS |
| Task 2 — Scan notifications | `ruppert_cycle.py` | ✅ PASS WITH WARNINGS |
| **Overall** | | **✅ PASS WITH WARNINGS** |

**CEO may approve both changes.** No blockers. Warnings are informational — W1 on Task 1 (outcomePrices scale) is the highest-priority item to watch on first real run in the signal window.

---

### Action Items for CEO

1. **Queue first live validation of Polymarket `outcomePrices` scale** — run `python fed_client.py` during next signal window (2026-03-14 review or after); inspect raw prices in the log. If they come back >1 (e.g. 97), the scale assumption is wrong and Developer must add a normalization guard.
2. **Optional cleanup ticket for Developer** — rename `"fedwatch_prob"` key in `ruppert_cycle.py` Step 4b opp dict to `"polymarket_prob"` for consistency.
3. **Track PST/PDT offset** — flag for Developer to make it dynamic (stdlib `datetime` can infer it from the system clock on Windows) before November 2026.
