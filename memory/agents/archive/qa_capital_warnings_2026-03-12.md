# QA Report — Capital Warning Fixes W1-W4 (2026-03-12)
_SA-4 QA | Reviewed: 2026-03-12_

**Status: ✅ PASS**

All four warning fixes are correctly implemented and verified. **W1**: `get_computed_capital()` in `logger.py` correctly falls back to `$400.0` and prints a warning to `stderr` when `total_deposits == 0.0` (file missing, empty, or entirely unreadable) — the check fires after the try/except block, covering both the "file doesn't exist" path (skips the open entirely) and the "file opens but yields no parseable records" path. **W2**: Both the deposits file `open()` and each trade log `open()` inside `get_computed_capital()` are individually wrapped in `try/except Exception` blocks that print descriptive warnings to `stderr` and continue gracefully; per-line `json.loads` errors remain protected as before. **W3**: The `get_computed_capital()` call in `run_weather_scan()` (`main.py`, line 252) is wrapped in a `try/except Exception` block that logs a warning to `stderr` and sets `total_capital = 400.0` — identical defensive pattern to `ruppert_cycle.py`. **W4**: `dashboard/api.py`'s `get_account()` demo branch now computes `STARTING_CAPITAL` inline (deposits sum → $400 fallback if zero → adds realized P&L from all `exit` records across `trades_*.jsonl` → `round(result, 2)`), correctly mirroring `get_computed_capital()` without importing from `logger.py`, with no circular import risk. No trading thresholds were modified in any file, no new bugs were introduced, all file opens use `encoding='utf-8'`, and no files outside the assigned scope were touched. Commit `50d32e4` is cleared for CEO approval.
