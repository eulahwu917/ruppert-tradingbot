# Dev Batch 2 — 2026-03-27b

**Agent:** SA-3 Developer  
**Date:** 2026-03-27  
**Status:** DONE — staged, not committed

---

## Fix 1: Secrets path (config.py + audit/security_audit.py)

**Problem:** Bot moved to `workspace/projects/ruppert-tradingbot-demo/` but paths still pointed one level up (`../secrets`) which resolves to nonexistent `workspace/projects/secrets/`.

**Fix:** Changed to `../../secrets` (two levels up → correct `workspace/secrets/`).

**Files changed:**
- `config.py` line 9: `'..', 'secrets'` → `'..', '..', 'secrets'`
- `audit/security_audit.py` line 131: `Path('../secrets')` → `Path('../../secrets')`

Both files staged in git.

---

## Fix 2: Git cleanup — staged deletions and additions

**Removed from tracking (git rm):**
- `PIPELINE.md` — moved to workspace root outside repo
- `bot/optimizer.py.bak` — git auto-detected rename to `archive/optimizer.py.bak` (shown as R)
- `memory/agents/qa_api_health_2026-03-13.md` — git auto-detected rename to `memory/agents/archive/` (shown as R)
- `memory/agents/qa_raw_results.json` — git auto-detected rename to `memory/agents/archive/` (shown as R)

**Added to tracking (git add):**
- `archive/arch_diagram.html` (A)
- `archive/optimizer.py.bak` (via rename from bot/)
- `archive/ruppert-backtest-old/` — entire old backtest folder (A, many files)
- `memory/agents/archive/` — archived QA files
- `memory/agents/qa-telegram-fix-2026-03-27.md` (A)

**Note:** `archive/ruppert-backtest-old/` was untracked and also got staged (it was in the filesystem, git added it when `git add archive/` ran). CEO should review if this was intended before committing.

---

## Status
- NOT committed. Staged only.
- Awaiting CEO commit after QA review.
