# Daily Notes — 2026-03-28

## 15-Min Crypto Binary Module — SHIPPED OVERNIGHT

David gave green light to build and activate in DEMO. Full pipeline ran: Researcher → Optimizer → Opus audit → CEO synthesis → Dev build → QA → activation.

### What was built
- **`crypto_15m.py`** — new standalone module for KXBTC15M, KXETH15M, KXXRP15M, KXDOGE15M
- 4-signal composite: TFI (0.40) + OBI (0.25) + MACD 5m (0.15) + OI delta (0.10)
- Sigmoid scale factor: 1.0 (to be calibrated from data — autoresearcher will tune)
- Timing gate: skip 0-2min, primary 2-8min, secondary 8-11min (1.25x edge req), block after 11min
- Min edge: 8c primary / 10c secondary
- 10 risk filters including Coinbase-Binance basis check (R10 — new)
- Decision logging to `logs/decisions_15m.jsonl` — ALL evaluations including skips
- Half-Kelly sizing, $100 hard cap, 4% daily cap

### Modified files
- `config.py` — CRYPTO_15M_MIN_EDGE, CRYPTO_15M_SIGMOID_SCALE, CRYPTO_15M_DAILY_CAP_PCT
- `position_monitor.py` — WS subscription includes 15m series; routing by ticker prefix
- `autoresearch.py` — crypto_15m added to domain list (needs 30 trades before it runs)

### Task Scheduler
- **Ruppert-WS-Persistent** added — runs `position_monitor.py --persistent` daily 6AM
- Persistent mode self-exits outside 6AM-11PM (confirmed working)
- Will activate tomorrow morning at 6AM

### Also shipped (earlier in session)
- Composite confidence aligned in WS path (Task 1 — morning session)
- WS investigation + `--persistent` flag (Task 2 — morning session)

### Key decisions to remember
- 15-min crypto on hold list removed — David said build and activate in DEMO
- Sigmoid scale 1.0 is intentionally uncalibrated — autoresearcher owns calibration
- RSI dropped from signal stack per Opus audit (wrong timescale for 15-min)
- Signal weights are FIXED, not to be tuned by Dev — autoresearcher only

### Tomorrow morning
- WS persistent session will start at 6AM
- First 15-min evaluations should start logging to decisions_15m.jsonl
- Check logs after 7AM full cycle to confirm routing is working
